# Bunhieng — shared core paste zone

**GitLab source of truth:** `frontend/src/components/` + `services/core/`  
**Your commit folder:** `tasks/bunhieng/` (mirror paths)

```powershell
.\scripts\paste-task.ps1 -Member bunhieng
```

After push: run `extract-tasks.ps1` so other members get updated slices.
