# Sorint — paste zone

**Copy path:** `tasks/sorint/*` → `frontend/src/*`

```powershell
cd "d:\Full Frontend"
.\scripts\paste-task.ps1 -Member sorint
cd frontend
npm run dev
```

**Git:** commit only `tasks/sorint/` (and matching `frontend/src/pages/auth`, etc. if you edit main app).

See [`GIT_WORKFLOW.md`](../../GIT_WORKFLOW.md)
